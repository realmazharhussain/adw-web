using Gtk 4.0;
using Adw 1;

template $AdwWebFolderItem : Adw.Bin {
  child: Box {
    orientation: horizontal;

    Image image {
      icon-name: bind template.icon-name;
      pixel-size: 24;
    }

    Label label {
      label: bind template.label;
    }
  };
}
